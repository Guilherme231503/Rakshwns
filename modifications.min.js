const runner = new ModifiedRunner(document.querySelector('canvas'), { difficulty: 'easy' });
runner.start();

// Activate speed boost
runner.activateSpeedBoost(2, 5000);

// Update weather inside game loop
function gameLoop() {
    runner.updateWeather();
    requestAnimationFrame(gameLoop);
}
gameLoop();
