// conteúdo do mod.js (o ModKit espera uma variável `mod` exposta)
var mod = {
  id: 'super-jump',
  onRegister(runner) {
    ModKit.API().log('super-jump registrado');
    // adiciona um comando simples: ao pressionar espaço duas vezes, aumenta velocidade
    runner.doubleJumpEnabled = true;
  },
  onTick(runner) {
    // por exemplo, se tecla X pressionada, ativa boost temporário
    // (este é apenas um exemplo — acessar input depende do jogo)
  }
};
